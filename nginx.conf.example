# ==============================================================================
# NGINX CONFIGURATION SNIPPET FOR GEO ROUTING SERVER
# ==============================================================================
# Добавьте подходящий блок конфигурации в ваш Nginx.
# ==============================================================================

# ------------------------------------------------------------------------------
# ВАРИАНТ 1 (СТАНДАРТНЫЙ): Выделенный поддомен (например, geo.example.com)
# Идеально для работы в связке с Remnawave / Marzban (исключает конфликты путей)
# ------------------------------------------------------------------------------
server {
    listen 443 ssl;
    server_name geo.example.com;

    # Сначала выпустите сертификат и проверьте эти пути.
    ssl_certificate /etc/letsencrypt/live/geo.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/geo.example.com/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ------------------------------------------------------------------------------
# ВАРИАНТ 2: Если Nginx и geo-routing-server работают в одной Docker-сети
# ------------------------------------------------------------------------------
# server {
#     listen 443 ssl;
#     server_name geo.example.com;
#     ssl_certificate /etc/letsencrypt/live/geo.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/geo.example.com/privkey.pem;
#
#     location / {
#         proxy_pass http://geo-routing-server:80;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }

# ------------------------------------------------------------------------------
# ВАРИАНТ 3: Проксирование отдельного пути на общем веб-сайте
# (Если корень 'location /' уже занят другим сайтом или сервисом)
# ------------------------------------------------------------------------------
# Способ 1: Прямой маршрут по вашему ROUTING_TOKEN из .env
# location /<ROUTING_TOKEN>/ {
#     proxy_pass http://127.0.0.1:8080;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
# }
#
# Способ 2: Регулярное выражение (не требует изменений при ротации токена)
# location ~ ^/[A-Za-z0-9_-]{16,64}/(HAPP|INCY)/ {
#     proxy_pass http://127.0.0.1:8080;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
# }
